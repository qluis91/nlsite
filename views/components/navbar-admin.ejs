<nav class="navbar navbar-admin">
  <div class="navbar-inner">
    <span class="navbar-brand"><%= site.name %> — Admin</span>
    <ul class="navbar-nav">
      <li><span class="nav-user-name"><i class="icon-user"></i> <%= user.name %></span></li>
      <li><a href="/" class="btn btn-sm btn-outline">Ver Sitio</a></li>
      <li>
        <form action="/auth/logout" method="POST" class="logout-form">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>">
          <button type="submit" class="btn btn-sm btn-outline logout-button">Cerrar Sesión</button>
        </form>
      </li>
    </ul>
  </div>
</nav>
